"""
config package — application-wide constants and configuration schemas.

Modules
-------
db_schema      — database name, CREATE TABLE DDL, and seed data
bloom_taxonomy — Bloom's taxonomy levels, verbs, and priority order
"""
